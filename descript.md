# MemorizerCLI - データ仕様

このドキュメントでは、MemorizerCLIで使用するデータ構造を説明します。

---

## 基本概念

MemorizerCLIは人間が読み書きしやすいtableという構造と、機械的に読み出しやすいdictという構造を用いて動作します。

---

## bookcodeとは

各データを識別するための書籍にユニークなコードです。

### 役割

- tableを統合する目印として
- 書籍・教材単位の抽象簡略化
- 表記揺れを気にしなくていい利便性

---

## code2title.json

bookcodeと書籍名の関連付けを定義するファイルです。

```json
{
  "eng001": "英単語帳A",
  "hist001": "世界史基礎"
}
```

---

## table仕様

人間が書きやすい単語リストです。後からdictへ統合することができるので、必要に応じて分割することができます。

```json
{
  "tabletitle": "基礎英単語_1-100",
  "bookcode": "eng001",
  "words": {
    "1": ["apple", "りんご"],
    "2": ["dog", "犬"],
    "3": "…"
  }
}
```

- `words` は dict
- keyは文字列の整数 (`"1"` であり、`1` ではない)
- valueは `[Word, Mean]` の2要素配列

---

## dictの仕様

launcherによって生成される統合データです。単一書籍ごとに統合するので、tableを参照するよりも検索が早いです。手間も減ります。

```json
{
  "bookcode": "eng001",
  "min_id": 1,
  "max_id": 1000,
  "count": 1000,
  "words": { "…":"…" }
}
```

---

### 単一書籍内での重複したwidの扱い

複数のtable内で単一書籍の同一widに対して2件以上の単語と意味のペアを定義しているときは、

* すべて完全一致 -> 許可
* 一つでも不一致 -> 警告（単語の衝突）、無視、継続

とします。

---

## 決め台詞

MemorizerCLIのデータ設計は

> 「単純化・分割・再構築」

この3点を軸に設計されています。